/**
 * DATEX II types in the exact JSON encoding defined by the schemas:
 * technical `G` suffixes, `{ value: ... }` enum wrappers and substitution
 * groups exactly as they appear on the wire.
 */
export * from './generated/wire.js';
export type { WireTypes } from './generated/wire-registry.js';
